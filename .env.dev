# Development Environment Variables
# Copy this to .env for local testing

# Database Configuration (for local development)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=zen_pipeline
DB_USER=zen_api
DB_PASSWORD=change-in-production-123

# Node.js Configuration
NODE_ENV=development
PORT=3001

# Domain Configuration (not used in dev mode)
DOMAIN=localhost
LETSENCRYPT_EMAIL=dev@localhost 